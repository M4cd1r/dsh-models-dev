{
  "name": "dsh-models-dev",
  "version": "0.1.4",
  "description": "Live models.dev catalog for DeepSeek Harness LLM providers: fetches provider/model metadata from models.dev and registers it through the dsh LLM seam — no stale vendored catalog. / Żywy katalog models.dev dla providerów LLM w DeepSeek Harness.",
  "type": "module",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/M4cd1r/dsh-models-dev.git"
  },
  "bugs": {
    "url": "https://github.com/M4cd1r/dsh-models-dev/issues"
  },
  "homepage": "https://github.com/M4cd1r/dsh-models-dev#readme",
  "exports": {
    ".": "./index.js",
    "./package.json": "./package.json"
  },
  "files": [
    "README.md",
    "README.zh.md",
    "LICENSE",
    "index.js",
    "lib/",
    "cordis.patch.yml"
  ],
  "scripts": {
    "check": "node --check index.js && node --check lib/runtime.mjs && node --check lib/map.mjs && node --check lib/sync.mjs && node --check lib/registry.mjs && node --check scripts/bootstrap.mjs && node --check test/bootstrap.test.mjs && node --check test/runtime.test.mjs",
    "test": "node --test test/*.test.mjs",
    "smoke": "node scripts/smoke.mjs",
    "bootstrap": "node scripts/bootstrap.mjs",
    "verify": "npm run check && npm test && npm run smoke"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "license": "MIT",
  "keywords": [
    "deepseek-harness",
    "dsh-plugin",
    "models-dev",
    "llm",
    "catalog",
    "opencode-go",
    "model-list"
  ],
  "dependencies": {
    "@deepseek-ai/schemastery": "^3.18.2"
  },
  "dsh": {
    "engines": {
      "dsh": ">=0.1.5-rc.1"
    },
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}
